1. Для практикума из занятия 7 продумать, где можно применить замыкания.
// все функции в js являются замыканиями
//можно использовать, чтобы переопределять значение переменной каждый раз при обработке функции внутри функции
//функции автоманически запоминают свой environment и имеют доступ к  внешним переменным


2. Не выполняя кода, ответить, что выведет браузер и почему:
if (!("a" in window)) {
    var a = 1;
}
alert(a); // undefined 


var b = function a(x) {
    x && a(--x);
};
alert(a); // a не было объявлено, выдаст ошибку


function a(x) {
    return x * 2;
}
var a;
alert(a); //выдаст тело функции


function b(x, y, a) {
    arguments[2] = 10;
    alert(a);
}
b(1, 2, 3); //10



function a() {
    alert(this);
}
a.call(null); //[object Window]